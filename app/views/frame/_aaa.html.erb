<script>
    $(document).ready(function () {
        $('#iframe_tab').IFrame('createTab', 'Home', '<%= users_home_path %>', 'home', true)
        //window.elem = document.getElementById('elem');
        window.iframe = $("#elem").find("iframe");
        iframe.id = "users_home"
        //$("#users_home")
        //const target = iframe.contentWindow.document.querySelector('#users_home');
        // iframeのDOMを取得


    })

    function parentFollowingIndexPageLink(link, user_name) {
        $('.content-wrapper').IFrame('createTab', user_name + 'フォロー中一覧', link, 'aaaaaaaaaa', true)
    }


</script>